{
  "name": "notification-service",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "engine": "bun --watch engine.ts",
    "start": "bun run index.ts",
    "start:engine": "bun run engine.ts",
    "prisma:generate": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "redis": "^5.12.1",
    "resend": "^6.14.0",
    "zod": "^4.4.3"
  }
}
